export enum UserRoleEnum {
  USER = 'user',
}

export enum ManagerRoleEnum {
  USER = 'user',
  MANAGER = 'manager',
}

export enum AdminRoleEnum {
  USER = 'user',
  MANAGER = 'manager',
  ADMIN = 'admin',
}
